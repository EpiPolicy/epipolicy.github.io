// configuration file for global settings

export const configuration = {
  mock_data: false,
  api_link: process.env.REACT_APP_API_ROOT_LINK,
  autoloadApplication: true
};

export default configuration;